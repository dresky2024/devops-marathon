# 1. Берем легкую базу (Alpine Linux)
FROM alpine:latest

# 2. Добавляем bash (в Alpine его нет по умолчанию)
RUN apk add --no-cache bash

# 3. Указываем рабочую папку внутри контейнера
WORKDIR /app

# 4. Копируем ВСЕ твои скрипты в контейнер
COPY *.sh ./

# 5. Даем права на запуск всем скриптам разом
RUN chmod +x *.sh

# 6. По умолчанию запускаем твой приветственный скрипт
CMD ["./hello.sh"]
