# 🚀 DevOps Marathon: От нуля до автоматизации

Мой практический путь изучения DevOps инструментов и методологий.

## 🛠 Что реализовано в проекте:

### 1. Bash Automation & Linux
* Написаны скрипты автоматизации на **Bash**.
* Реализовано логирование системных событий и работа с переменными окружения.

### 2. Git & GitHub
* Настроено управление версиями.
* Использование `.gitignore` для соблюдения "чистоты" репозитория.

### 3. Docker & Containerization
* Создание кастомных **Dockerfiles**.
* Проброс портов и монтирование томов (**Volumes**) для локальной разработки.
* Упаковка веб-сервера **Nginx** в контейнер.

### 4. Docker Compose
* Описана мульти-контейнерная архитектура (веб-сервис + сервис статистики).
* Запуск всей инфраструктуры одной командой `docker-compose up`.

### 5. CI/CD (GitHub Actions)
* Настроены автоматические пайплайны для сборки Docker-образов.
* Реализованы тесты для проверки наличия контента внутри образов при каждом `push`.

### 6. Ansible (Infrastructure as Code)
* Написаны **Playbooks** для автоматической настройки веб-серверов.
* Реализована идемпотентная доставка контента на сервер.

## 📈 Как запустить проект локально:
1. Клонировать репозиторий: `git clone <ссылка>`
2. Запустить веб-проект: `cd web-project && docker build -t my-web .`
3. Поднять Compose-стек: `cd compose-project && docker-compose up -d`
